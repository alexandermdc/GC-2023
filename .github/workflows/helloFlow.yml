name: Hello Flow
on:
   schedule:
     - cron: '*/5 * * * *'
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: diga oi
        run: echo "Hello World!"
      - name: diga valeu
        run: echo "Valeu!"
